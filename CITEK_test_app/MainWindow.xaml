<Window x:Class="CITEK_test_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CITEK_test_app"
        mc:Ignorable="d"
        Title="Тестовое приложение" Height="800" Width="1000" MinHeight="400" MinWidth="500">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Name="LogTextBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" IsReadOnly="True" Text="{Binding Log, Mode=OneWay}" Style="{StaticResource CommonTextBoxStyle}" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="Прогресс загрузки данных:" Style="{StaticResource CommonTextBlockStyle}"/>
        <ProgressBar Name ="DownloadProgressBar" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource CommonProgressBarStyle}" Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="1"/>

        <Button Name="UpdateDataButton" Content="Загрузить данные" Grid.Row ="3" Grid.Column="0" Style="{StaticResource CommonButtonStyle}" IsEnabled="{Binding UpdatingDataAllowed, Mode=OneWay}"/>
        <Button Name="SaveDataInReportButton" Content="Создать отчёт" Grid.Row ="3" Grid.Column="1" Style="{StaticResource CommonButtonStyle}" IsEnabled="{Binding CreatingReportAllowed, Mode=OneWay}"/>
    </Grid>
</Window>
